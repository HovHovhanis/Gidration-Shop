
// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//     products: [],
//     userInfo: null,
// }

// export const gidrationSlice = createSlice({
//     name: 'gidration',
//     initialState,
//     reducers: {
//         addToCart: (state, action) =>{
//             const item = state.products.find((item) => item.id === action.payload.id)
//             if (item) {
//                 item.quantity += action.payload.quantity
//             } else {
//                 state.products.push(action.payload)
//             }
//         }
//     }
// })

// export const { addToCart } = gidrationSlice.actions;

// export default gidrationSlice.reducer;

